const Fruits: string[] = ["Apple", "Banana", "Orange", "Grapes"];

console.log("Using a while loop to iterate over the array:");

let currentIndex = 0;

while (currentIndex < Fruits.length) {
    console.log(Fruits[currentIndex]);
    currentIndex++;
}
