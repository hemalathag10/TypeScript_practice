const numbers: number[] = [1, 2, 3, 4, 5];

console.log("Using a for loop to iterate over the array:");
for (let i = 0; i < numbers.length; i++) {
    console.log(numbers[i]);
}
OUTPUT :
Using a for loop to iterate over the array:
1
2
3
4
5
